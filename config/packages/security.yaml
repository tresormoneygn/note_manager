security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
                default_target_path: app_home
            logout:
                path: app_logout
                target: app_login
            custom_authenticator: App\Security\UserAuthenticator
            entry_point: form_login
            access_denied_handler: App\Security\AccessDeniedHandler
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800
                path: /
                # by default, the feature is enabled by checking a checkbox in the
                # login form, uncomment the following line to always enable it.
                #always_remember_me: true
                # where to redirect after logout
                # target: app_any_route

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    
    role_hierarchy:
        ROLE_P: ROLE_USER
        ROLE_DG: ROLE_USER
        ROLE_DGA_E: ROLE_USER
        ROLE_DGA_R: ROLE_USER
        ROLE_SF: ROLE_USER
        ROLE_CD: ROLE_USER
        ROLE_DP: ROLE_USER
    
    access_control:
        # Routes d'inscription (réservées au DG)
        - { path: ^/register, roles: ROLE_DG }
        
        # Routes de gestion des programmes et départements (réservées au DG)
        - { path: ^/programme/new, roles: ROLE_DG }
        - { path: ^/programme/.*/edit, roles: ROLE_DG }
        - { path: ^/departement/new, roles: ROLE_DG }
        - { path: ^/departement/.*/edit, roles: ROLE_DG }
        
        # Routes de gestion des matières et unités d'enseignement (réservées au DGA/E)
        - { path: ^/matiere/new, roles: ROLE_DGA_E }
        - { path: ^/matiere/.*/edit, roles: ROLE_DGA_E }
        - { path: ^/unite/enseignement/new, roles: ROLE_DGA_E }
        - { path: ^/unite/enseignement/.*/edit, roles: ROLE_DGA_E }
        
        # Routes de gestion des notes (réservées aux professeurs)
        - { path: ^/note, roles: ROLE_P }
        
        # Routes publiques
        - { path: ^/login, roles: PUBLIC_ACCESS }
        - { path: ^/$, roles: PUBLIC_ACCESS }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
