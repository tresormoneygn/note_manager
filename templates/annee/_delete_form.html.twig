<form method="post" action="{{ path('app_annee_delete', {'id': annee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annee.id) }}">
    <button class="btn">Delete</button>
</form>
