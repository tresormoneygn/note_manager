<form method="post" action="{{ path('app_departement_historique_delete', {'id': departement_historique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departement_historique.id) }}">
    <button class="btn">Delete</button>
</form>
