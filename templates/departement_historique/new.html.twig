{% extends 'base.html.twig' %}

{% block title %}New DepartementHistorique{% endblock %}

{% block body %}
    <h1>Create new DepartementHistorique</h1>
    {# Affichage des messages flashs #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    {{ include('departement_historique/_form.html.twig') }}

    <a href="{{ path('app_departement_historique_index') }}">back to list</a>
{% endblock %}
