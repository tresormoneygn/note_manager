{% extends 'base.html.twig' %}

{% block body %}
    <h1>Importer un rapport Excel</h1>

    {{ form_start(form) }}
    {{ form_row(form.rapport) }}
    <button class="btn btn-primary">Importer</button>
    {{ form_end(form) }}
{% endblock %}
