<form method="post" action="{{ path('app_unite_enseignement_delete', {'id': unite_enseignement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unite_enseignement.id) }}">
    <button class="btn">Delete</button>
</form>
